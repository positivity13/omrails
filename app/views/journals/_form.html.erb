<%= form_for(journal) do |f| %>
  <% if journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journal.errors.count, "error") %> prohibited this journal from being saved:</h2>

      <ul>
      <% journal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    
    <h2>I am grateful for...</h2>
    <%= f.text_area :content1, class: "form-control", placeholder: "1." %>
    <%= f.text_area :content2, class: "form-control", placeholder: "2." %>
    <%= f.text_area :content3, class: "form-control", placeholder: "3." %>
    
  </div>
  <div class="form-group">
    <h2>What would make today great?</h2>
    <%= f.text_area :content4, class: "form-control", placeholder: "1." %>
    <%= f.text_area :content5, class: "form-control", placeholder: "2." %>
    <%= f.text_area :content6, class: "form-control", placeholder: "3." %>
  </div>

  <div class="form-group">
    <h2>Daily affirmations</h2>
    <%= f.text_area :content7, class: "form-control", rows: "5", placeholder: "I am ..." %>
  </div>

  <%= f.submit class: "btn btn-warning" %>
  
<% end %>
